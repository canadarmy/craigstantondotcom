#from django import forms

"""
class BlockchainForm(forms.Form):
    BC_CHOICES = (
        ('COIN', 'Buy Coins'),
        ('CONTRACT', 'Contract Craig'),
        ('TEST', 'Test Blockchain'),
    )
    select = forms.MultipleChoiceField(label='Blockchain Choices'
        required=False,
        widget=forms.RadioSelect, choices = BC_CHOICES
    )
"""
